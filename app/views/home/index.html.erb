
<% if current_user %>
  <h1>My Groups</h1>
  <div id="ownedGroups">
    <%= render :partial => 'groups/list_own', :locals => {:groups_owned => current_user.own_groups} %>
  </div>
  
  <div id="memberGroups">
    <%= render :partial => 'groups/list_member' %>
  </div>
<% end %>
